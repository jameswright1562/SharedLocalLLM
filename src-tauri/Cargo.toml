[package]
name = "shared-local-llm"
version = "0.1.1"
description = "Secure two-node local LLM orchestration"
authors = ["SharedLocalLLM contributors"]
edition = "2021"

[lib]
name = "shared_local_llm"
path = "src/lib.rs"

[[bin]]
name = "shared-local-llm"
path = "src/main.rs"

[build-dependencies]
ico = "0.5"
tauri-build = { version = "2", features = [] }

[dependencies]
base64 = "0.22"
dirs = "6"
futures-util = "0.3"
get_if_addrs = "0.5"
glob = "0.3"
hex = "0.4"
rand = "0.8"
regex = "1"
reqwest = { version = "0.12", default-features = false, features = ["json", "stream", "rustls-tls"] }
rfd = "0.15"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
snow = "0.9"
sysinfo = "0.33"
tauri = { version = "2", features = ["tray-icon"] }
thiserror = "2"
tokio = { version = "1", features = ["fs", "io-util", "macros", "net", "process", "rt-multi-thread", "sync", "time"] }
uuid = { version = "1", features = ["v4", "serde"] }
walkdir = "2"
zip = { version = "2", default-features = false, features = ["deflate"] }

[target.'cfg(windows)'.dependencies]
windows = { version = "0.61", features = ["Win32_Foundation", "Win32_Security_Cryptography", "Win32_System_Memory"] }

[dev-dependencies]
tempfile = "3"
